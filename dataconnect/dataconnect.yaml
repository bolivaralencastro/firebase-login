specVersion: "v1beta"
serviceId: "firebase-login"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-login-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
